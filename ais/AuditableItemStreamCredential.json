{
	"type": "object",
	"properties": {
		"@context": {
			"type": "array",
			"minItems": 1,
			"items": [
				{
					"type": "string",
					"const": "https://schema.twindev.org/ais/"
				}
			],
			"additionalItems": {
				"type": "string"
			},
			"description": "JSON-LD Context."
		},
		"type": {
			"type": "string",
			"const": "AuditableItemStreamCredential",
			"description": "JSON-LD Type."
		},
		"created": {
			"type": "number",
			"description": "The timestamp of when the stream was created."
		},
		"userIdentity": {
			"type": "string",
			"description": "The identity of the user which added the stream."
		},
		"hash": {
			"type": "string",
			"description": "The hash of the stream."
		},
		"signature": {
			"type": "string",
			"description": "The signature of the stream."
		}
	},
	"required": ["@context", "type", "created", "userIdentity", "hash", "signature"],
	"additionalProperties": false,
	"description": "Interface describing the immutable credential for a stream."
}
