{
	"type": "object",
	"properties": {
		"@context": {
			"anyOf": [
				{
					"type": "string",
					"const": "https://schema.twindev.org/ais/"
				},
				{
					"type": "array",
					"minItems": 1,
					"items": [
						{
							"type": "string",
							"const": "https://schema.twindev.org/ais/"
						}
					],
					"additionalItems": {
						"type": "string"
					}
				}
			],
			"description": "JSON-LD Context."
		},
		"type": {
			"type": "string",
			"const": "AuditableItemStreamVerification",
			"description": "JSON-LD Type."
		},
		"id": {
			"type": "string",
			"description": "The id, only used if the verification if for an entry."
		},
		"state": {
			"$ref": "https://schema.twindev.org/ais/AuditableItemStreamVerificationState",
			"description": "The state of the verification."
		}
	},
	"required": ["@context", "type", "state"],
	"additionalProperties": {},
	"description": "Interface describing an auditable item stream verification."
}
