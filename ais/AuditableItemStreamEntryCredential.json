{
	"type": "object",
	"properties": {
		"@context": {
			"anyOf": [
				{
					"type": "string",
					"const": "https://schema.twindev.org/ais/"
				},
				{
					"type": "array",
					"minItems": 1,
					"items": [
						{
							"type": "string",
							"const": "https://schema.twindev.org/ais/"
						}
					],
					"additionalItems": {
						"type": "string"
					}
				}
			],
			"description": "JSON-LD Context."
		},
		"type": {
			"type": "string",
			"const": "AuditableItemStreamEntryCredential",
			"description": "JSON-LD Type."
		},
		"dateCreated": {
			"type": "string",
			"description": "The date/time of when the stream was created."
		},
		"userIdentity": {
			"type": "string",
			"description": "The identity of the user which added the entry to the stream."
		},
		"hash": {
			"type": "string",
			"description": "The hash of the entry."
		},
		"signature": {
			"type": "string",
			"description": "The signature of the entry."
		},
		"index": {
			"type": "number",
			"description": "The index of the entry in the stream."
		}
	},
	"required": ["@context", "type", "dateCreated", "userIdentity", "hash", "signature", "index"],
	"additionalProperties": false,
	"description": "Interface describing the immutable credential for an entry in the stream."
}
