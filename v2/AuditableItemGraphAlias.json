{
	"type": "object",
	"additionalProperties": false,
	"properties": {
		"metadata": {
			"$ref": "https://schemas.gtsc.io/v2/JsonLdDocument"
		},
		"id": {
			"type": "string",
			"description": "The id of the element."
		},
		"created": {
			"type": "number",
			"description": "The timestamp of when the element was created."
		},
		"updated": {
			"type": "number",
			"description": "The timestamp of when the element was updated."
		},
		"deleted": {
			"type": "number",
			"description": "The timestamp of when the element was deleted, as we never actually remove items."
		}
	},
	"required": [
		"created",
		"id"
	],
	"description": "Interface describing an alias for a vertex."
}