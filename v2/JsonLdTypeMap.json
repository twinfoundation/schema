{
	"type": "object",
	"additionalProperties": {
		"anyOf": [
			{
				"type": "string"
			},
			{
				"$ref": "https://schema.gtsc.io/v2/JsonLdNodeObject"
			}
		]
	},
	"description": "A type map is used to associate an IRI with a value that allows easy programmatic access."
}